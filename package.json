{
    "name": "super-new-file",
    "displayName": "super-new-file",
    "description": "super new file",
    "version": "0.0.1",
    "publisher": "cg-cnu",
    "engines": {
        "vscode": "^1.16.0"
    },
    "categories": [
        "Other"
    ],
    "activationEvents": [
        "onCommand:superNewFile.create"
    ],
    "main": "./out/src/superNewFile",
    "contributes": {
        "commands": [
            {
                "command": "superNewFile.create",
                "title": "Super New File"
            }
        ]
    },
    "scripts": {
        "vscode:prepublish": "npm run compile",
        "compile": "tsc -p ./",
        "watch": "tsc -watch -p ./",
        "postinstall": "node ./node_modules/vscode/bin/install",
        "test": "npm run compile && node ./node_modules/vscode/bin/test"
    },
    "devDependencies": {
        "typescript": "^2.5.2",
        "vscode": "^1.1.5",
        "mocha": "^3.5.0",
        "@types/node": "^7.0.43",
        "@types/mocha": "^2.2.42"
    },
    "dependencies": {
        "mkdirp": "^0.5.1",
        "node-mkdirfilep": "^0.0.2"
    }
}
